# TabListEnhancer-Velocity
Add some tab list enhancement to velocity

Splitted from [lls-manager(LBS fork)](https://github.com/Lazy-Bing-Server/lls-manager)

## Config
`config.yml` in its data directory.

Set `enabled` to true to enable this plugin

Set `tablist-update-interval` to change tab list update interval in milliseconds
